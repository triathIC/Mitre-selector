node_modules/
dist/
dist-node/
.vite/
*.tsbuildinfo
*.local
.DS_Store
.env
.env.local
.env.production
.env*.local

# Obsidian PKM — nicht mit Repo syncen
docs/PROJECT-DOCUMENTATION.md
claude.md

# Claude Code — lokal, nicht versioniert
.claude/
REVIEW.md

# Raw MITRE STIX bundle — pinned via data/mitre-manifest.json (SHA-256),
# re-download with `bash scripts/update-mitre.sh`
data/enterprise-attack.json
